# Normalise to LF in the repository. The bin entry carries a shebang and must
# not end up with CRLF, which would break execution on macOS and Linux.
* text=auto eol=lf
*.png binary
*.jpg binary
*.gz binary
*.tgz binary
